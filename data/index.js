const recipeData = require("./recipes");
const userData = require("./users");
const imageData = require("./images");

module.exports = {
    recipes : recipeData,
    users : userData,
    images : imageData
}